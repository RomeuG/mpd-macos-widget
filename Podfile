# Uncomment the next line to define a global platform for your project
platform :osx, '10.14'

target 'mpd-whatsplaing-extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mpd-whatsplaing-extension
  pod 'SwiftSocket'
end

target 'mpd-whatsplaying' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mpd-whatsplaying

end
